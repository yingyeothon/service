{
  "name": "@yyt/service-auth",
  "version": "0.0.0",
  "private": true,
  "description": "auth.yyt.life — per-channel OAuth login (GitHub/Google) issuing HS256 channel JWTs",
  "type": "module",
  "scripts": {
    "build": "echo 'bundled by serverless-esbuild at deploy time'",
    "typecheck": "tsc --noEmit",
    "deploy:dev": "serverless deploy --stage dev --region ap-northeast-2"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.864.0",
    "@yyt/console-db": "workspace:*",
    "@yyt/core": "workspace:*",
    "@yyt/http": "workspace:*",
    "@yyt/jwt": "workspace:*",
    "@yyt/sqlite-s3": "workspace:*",
    "@yyt/upstash": "workspace:*",
    "jose": "^6.0.12",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.2.0",
    "esbuild": "^0.25.9",
    "serverless-domain-manager": "^8.0.0",
    "serverless-esbuild": "^1.55.0"
  }
}
