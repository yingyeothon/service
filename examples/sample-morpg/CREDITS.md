# Credits

## Art (`assets/view/`)

- `tiles.png` / `tiles.json` and `actors.png` / `actors.json` are packed by
  `scripts/pack-assets.mjs` from two 16 px sheets (a 1024×1024 tileset and a
  256×128 character sheet) that the repository owner (lacti) generated with
  ChatGPT image generation (OpenAI) in August 2026, from the owner's own
  prompts. No third-party asset pack, stock art or copyrighted reference image
  was used as input.
- Rights position: under the OpenAI Terms of Use the user owns the output, so
  whatever rights exist in these images belong to the owner. Purely
  AI-generated images may not qualify for copyright protection in some
  jurisdictions (e.g. the US Copyright Office requires human authorship); no
  claim is made either way.
- Licence: to the extent the owner holds any copyright or related rights in the
  files under `assets/view/`, they are dedicated to the public domain under
  [CC0-1.0](https://creativecommons.org/publicdomain/zero/1.0/). Where no such
  rights exist, the files are public domain regardless. Attribution is welcome
  but not required. This covers only `assets/view/`; the rest of the
  repository is licensed separately.
- Not covered: the OpenAI service marks; the raw source sheets and their
  mapping JSON stay outside the repository (gitignored `local/`).
- No further art is added; entities without their own drawing use substitutes
  declared in the bundles' `view` sections (e.g. the wolf is a tinted bat).

## Data

- `assets/zone*.json` and everything under `src/` are original to this repository.
