# Golden review evidence (2026-08-28)

Review of this repo + `~/git/yyt.life/tslib` against the golden platform doc (`yyt-platform.md`, kept outside this public repo; retired 2026-09-08 after a four-repo audit found it satisfied — its durable content is `docs/decisions.md`, `docs/realtime-gateway-design.md` and `gateway/README.md`) §7 invariants and §8 constraints, run after the gateway 0.2.0 / tslib 2.0.0 release. Verdict rules: `holds` (cite code + test), `holds-untested` (add a test or record why not), `violated` (fix; edit `docs/decisions.md` first if a decision changes). Follow-ups that stayed open moved to `todo/19` at the time; the review itself closed 2026-08-28.

> 2026-09-03: `examples/sample-*` moved to the `yingyeothon/examples` repo (`~/git/yyt.life/examples`); `examples/*` paths below are as they were. Line numbers are as of 2026-08-28.

## §7 invariants

| #   | Invariant                                                 | Verdict              | Evidence / follow-up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Gateway never parses a game payload                       | holds                | `gateway/internal/lobby/hub.go:345-363` (typed set: ping/pos/say/event/party.*), `event.payload` is `json.RawMessage` copied unread (`protocol.go:19`, `hub.go:633`, 8 KB cap); q: `internal/q/bridge.go:326-373` reads only `type`, strips `memberId`, re-encodes the rest. Tests `hub_test.go:273-289`, `bridge_test.go:236-247`, smoke `gateway.mjs:279-284`. Note: q opacity is field-level (object round-trip), not byte-level.                                                                                                                                                                                                                                   |
| 2   | `hello` first; map change = config edit only              | holds                | `hub.go:210-212` (inside join lock), shape `protocol.go:24-36` from `h.cfg`; SDK `gamebase-client/src/gateway-socket.ts:276-284,331-337` (hello-first + timeout), `lobby-client.ts:310-326` (`connect()` resolves with hello, `map()` fetches `hello.mapUrl`). Tests `hub_test.go:422` (`TestHelloIsFirstEvenWithTraffic`), `lobby-client.test.ts:80-128`; smoke `morpg.mjs:202-213`→`457-462` patches `config.mapUrl` via console and asserts hello carries it.                                                                                                                                                                                                       |
| 3   | Client writes nothing durable (apiKey server-side only)   | holds                | `services/state/src/app.ts:144-147` (`requireServer` on PUT `:193` / DELETE `:249`), owner read scope `:180-181`; credential kinds `services/state/src/channels.ts:63-97`. Tests `services/state/test/app.test.ts:118,216,317,101,359`.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 4   | Every Redis key has a TTL, debug keys included            | holds (convention)   | Every service write passes `ex`/`expire` (match `pool.ts:103-112`, `debug.ts:73-77`; topic `topics.ts:94-138`; console `session.ts`, `channel-redis.ts:180-184`, `gateway-probe.ts:118-141`, `usage-digest.ts:315`; auth `state.ts:32`); Go `redisx.go:100,111,134,157,233,253`. Gaps kept as lessons: no lint enumerates write sites; tslib `actor-system-redis/queue.ts:52-62` `ttlSeconds` optional and `repository-redis` `set` TTL-less — in this platform only the gateway RPUSHes (TTL'd) and both samples pass a TTL to `saveActorStartEvent`; `redisx.go:96-101` GETSET+EXPIRE is two round trips. (Both tslib gaps were closed 2026-08-29, tslib `1832288`.) |
| 5   | Every doc write is version-conditional                    | holds                | `packages/console-db/src/state.ts:154-175` (`updateMany … where version`), create `:130-153`, delete `:180-190`; 409 mapping `services/state/src/app.ts:97-117`, `If-Match` mandatory (`428`, `*` refused) `:149-167`. Tests `app.test.ts:170,177,185,199,208,297`. tslib `repository/src/document/map.ts:35-45` had no CAS (golden §8; CAS added 2026-08-29, tslib `faff8df`); sample-morpg uses the doc service with retry-on-409 (`src/doc.ts:56-78`, `src/commit.ts`).                                                                                                                                                                                             |
| 6   | Inbound durable, outbound lossy + self-contained frames   | holds                | RPUSH `bridge.go:409-419`→`redisx.go:230-232`; SUBSCRIBE `bridge.go:229`, `outboundLoop` `:500-513`; publisher `lambda-gamebase/src/network/createRedisPubSubTransport.ts:37-56`. Frame = full players+monsters+time+cleared `sample-morpg/src/sim.ts:457-490`, every tick `src/actor.ts:259-261`, private resync on join `:234-258`. Test `sim.test.ts:142,223-227`. Note: `events` are drained into the frame (exactly-once over lossy) — decorative only; no test for a dropped PUBLISH (kept as an examples-repo backlog item).                                                                                                                                    |
| 7   | Dead actor: bounded queue, abort ≠ normal end (4001/1000) | holds                | `bridge.go:35-42` (`DepthCap=200`, `NoProgress=5s`), trip `:429-437`, `abort()` `:456-478` → `conn.CloseActorUnavailable`=4001 (`conn.go:22`), DEL queue; normal end 1000 (`:539`). Backpressure drops oldest `conn.go:142-149`. Tests `bridge_test.go:181-227,236-258`, `conn_test.go:89-104`. UNSUBSCRIBE follows via Leave→`dropGameLocked` (`:311-315,477-490`); comment fixed 2026-08-28.                                                                                                                                                                                                                                                                         |
| 8   | Wrong key prefix fails loudly (`NOPERM`)                  | holds-untested       | ACL user `packages/redis/src/aclAdmin.ts:236-275` (`resetkeys ~game:{stage}:{id}:* resetchannels &game:out:{stage}:{id}:*`, `-@dangerous -scan …`), prefixes derived `services/console/src/channels.ts:299-313`. Tests `aclAdmin.test.ts:42-60`, `console/test/gateway.test.ts:97-103,298-299`, `cli/internal/cmd/channels_redis_test.go:37`. NOPERM itself is a manual host check (`rules/data.md`); no smoke — acceptable (needs a second credential on the box). SPA drift fixed 2026-08-28: `ChannelDetail.tsx` showed 8 `CopyField`s; now one `CopyBlock` (`name=value` lines) per `docs/decisions.md` _Participant credentials_.                                 |
| 9   | Superseded connection is closed (4000)                    | holds                | lobby `hub.go:199-204`, q `bridge.go:270-277`, code `conn.go:21`; SDK maps 4000 → stop (`close-codes.ts:7,34-35`). Tests `hub_test.go:198`, `server_test.go:219-232`, `bridge_test.go:166-178`, `game-client.test.ts:144`; smoke `gateway.mjs:288,313-320`. q replacement has no smoke (unit only).                                                                                                                                                                                                                                                                                                                                                                    |
| 10  | No credential / request event / connection-id list logged | holds-untested       | `rules/security.md`; only three credential-adjacent log sites, ids/outcomes only (`console/src/handler.ts:179`, `gateway.ts:60`, `auth/src/app.ts:133`); state logs `{channelId, kind}` `app.ts:285-287`; Go: driver errors are logged unmodified (a dial failure names the host, accepted per `rules/security.md`; address masking added 2026-08-30). One test `channel-redis.test.ts:175-188`. Enforcement stays review + rule; no lint (lesson recorded).                                                                                                                                                                                                           |
| 11  | `reservedConcurrency` on every Lambda                     | **violated → fixed** | Platform stacks all set (`services/*/serverless.yml`). Missing on all 7 `examples/*` functions — added 2026-08-28, sized to the flow's peak after review (morpg http 20 / actor 10; dungeon authorizer 20 / ws 20 / actor 10 / callbacks 5 — a 16-member party bursts one uncached authorizer + `$connect` each, and morpg `http` serves the blocking `enter` and the actor's `ready` callback from one pool), dev morpg redeployed, golden §7-11 now names `examples/*`. A root test (`test/serverless-invariants.test.ts`, added 2026-08-30) pins the invariant for this repo's stacks.                                                                              |
| 12  | No `character`/`item`/`quest`/`party` schema in platform  | holds                | `packages/console-db/prisma/schema.prisma` — no such model/column; docs are opaque `state_docs.body` (`state.ts:67-75`, `app.ts:198-205`). `party` exists only as relay scope config (`channels.ts:53-66`), Redis 30 m keys (`redisx.go:149-159`), and an opaque `ownerId` namespace (`app.ts:34-41`). Tests `app.test.ts:143,240`.                                                                                                                                                                                                                                                                                                                                    |

## Closing notes

- §8 constraints were re-checked against the private ops repo (box facts — addresses, sizes, limits — live in gitignored `local/ops-notes.md` and that repo, not here); the golden doc's §2/§8 were corrected where stale.
- `examples/sample-morpg/README.md` §7 walk: items 1–11 passed on dev (`morpg.mjs` ALL OK 2026-08-28); 12 implemented but not smoke-tested; 13 (gateway restart mid-dungeon), 14 (q-mode double socket — unit-tested only), 15 (full 12-minute run) never exercised — kept optional.
- Follow-ups spawned by this review (tslib fixes, `redisx` atomicity, address masking, the invariants test) were tracked in `todo/19` and are individually dated above where closed.
