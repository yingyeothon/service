node_modules/
dist/
.serverless/
.esbuild/
coverage/
*.tsbuildinfo
.env
.env.*
# Everything under local/ is machine-local (credentials, deploy params); only the README and example are tracked.
local/*
!local/README.md
!local/env.example
.envrc
!.envrc.example
cli/dist/
apps/console-web/dist/
.claude/*
!.claude/settings.json
.codex/
.DS_Store
# Work tracker is machine-local (contains infra notes); never tracked.
todo/
# Prisma-generated client (rebuilt by `prisma generate` in the package build).
packages/console-db/src/generated/
# Local `go build ./...` output of the CLI binary.
cli/yyt
