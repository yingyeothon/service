# Manual Verification

- After tests pass, deploy the touched stack to `dev` (`scripts/deploy.sh <service> dev`) and exercise the real path: curl for httpApi, `wscat -c <wss> -s bearer -s <jwt>` for WebSocket, the match stack's dev-only `POST /debug/callback` as the matchmaker callback sink (`scripts/smoke/match.mjs`).
- `serverless-offline` is not used (poor WebSocket fidelity); `dev` on AWS is the controllable target. Cost is negligible at this traffic.
- Debug-only hooks (active only when `STAGE=dev` and `DEBUG_HOOKS=1`): seed a channel with a known secret, mint a test JWT (`POST /debug/token`), force-expire a topic, trigger `tryMatch`/`expire` on demand. They must be absent from `prod` deployments (guard at handler registration, not just at runtime).
- Record the exact commands used in the task's todo doc so the next session can repeat them.
- Stage prerequisites (per stage, once): MySQL/Redis accounts from `yyt-stateful` in `local/env/*.{stage}.env`, then `scripts/bootstrap-ssm.sh <stage>` (SSM `/yyt-service/{stage}/{service}/*`, `gateway-token` on every stage, `debug-key` and `auth/debug-mysql-*` on dev). `gateway-ws-url` is set by hand once the gateway host resolves. `serverless.yml` fails loudly on missing params by design. The S3 bucket `yyt-service-{stage}` remains for posters/backups (07).
- Artifact CDN CORS: `node scripts/smoke/cdn-cors-browser.mjs <cdn url of the sample's MAP_URL>` (headless Chrome over CDP, `ROUNDS=3`). `curl`/node see other edge variants than Chrome; only this counts (`rules/deployment.md`, 2026-09-05).
- Static sites: `node scripts/smoke/sites.mjs https://console-dev.yyt.life <debugKey>` (site CRUD, presigned zip → commit 202 → poll → the host serves `index.html`/`config.json` with the right headers, a failing zip, a second deploy that prunes, delete). Its `nosniff` check needs the response-headers policy attached by hand (`todo/07`). The sample's hosted client: `pnpm web:build && pnpm web:config -- --config <env>` then `yyt site deploy morpg-web web/dist --exclude '*.map'` (a site name needs the project context: `--team`/`--project`, or a `.yyt.json`; the locally installed `yyt` may predate `site` — rebuild from `cli/`), driven by `local/tools/morpg-web-drive.mjs` (now in `yingyeothon/examples` `local/tools/`) with `MORPG_WEB_URL=https://dev-g.yyt.life/<slug>/`.
- Opt-in integration tests hit the real dev instances: `YYT_IT=1 pnpm test` (skipped when `local/env/*.dev.env` is absent). Run them before deploying a storage change.
- auth smoke: `scripts/deploy.sh auth dev --param debugHooks=1` then `scripts/smoke/auth.mjs https://auth-dev.yyt.life "$(cat local/deploy/debug-key.dev)"`.
- Smoke scripts share `scripts/smoke/_lib.mjs` (2026-09-02): `createChecker()` for `check`/`failed()`/`finish`, `jsonClient({base, writeSlotMs, redirect, timeoutMs})` for the fetch helper (pass `writeSlotMs: 550` for a script that records console writes, `redirect: "manual"` when a 302 must be seen), `debugLogin`/`consoleLogin`/`asUser`, `mintToken`, `wsConnector({nextMs, untilMs, closeMs, handshakeMs})` + `wsRejected`, `refusedUpgrade` and `exitOnCrash()`. A new script starts from those; the values are per script on purpose, so copy the neighbour's options, not its code.
- Smoke scripts are ESM entry modules driven by top-level `await`: on Node >= 22 a rejected top-level await is an `uncaughtException`, not an `unhandledRejection`, so a script that only installs the latter never prints its `N FAILED` summary on a crash — install both (`morpg.mjs` `crashed`). Raw `http.request` upgrade probes need `req.setTimeout` or a stalled gateway hangs the run.
- Every smoke `setup` starts with console's `POST /debug/login`; a console dev deploy without `--param debugHooks=1` (e.g. after a prod-then-dev rollout) makes it fail with `401` on every console call while the auth seed still passes — redeploy console dev with the param first.
- console smoke: `scripts/deploy.sh console dev --param debugHooks=1` then `GATEWAY_TOKEN="$(cat local/deploy/gateway-token.dev)" scripts/smoke/console.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life`. Without `GATEWAY_TOKEN` the `GET /gw/channels` checks are skipped and the run still passes green — pass it, or you are not testing that route. `POST /debug/login` (dev only) mints a session for a synthetic member (`dbg_*`, negative `github_id`) so the API is testable without a GitHub OAuth app; the script cleans up its channels/tokens and demotes the synthetic member back to `pending`. Since the 30-day name hold on soft-deleted channels (2026-09-02, `55ff2ba`) its fixed rename target `renamed` collides with the previous run's deleted channel and the `patch` check fails with `conflict … still holds the name` — a smoke defect, not a service one (`todo/29`); until the script stamps the name, read that one FAIL as known.
- gateway smoke: run the gateway locally against the dev console/auth and a throwaway Redis (`docker run -d --rm -p 127.0.0.1:6390:6379 redis:7-alpine`; `cd gateway && go build -o gateway ./cmd/gateway && GATEWAY_STAGE=dev GATEWAY_CONSOLE_URL=https://console-dev.yyt.life GATEWAY_TOKEN_FILE=../local/deploy/gateway-token.dev GATEWAY_REDIS_URL=redis://127.0.0.1:6390/0 GATEWAY_LISTEN=127.0.0.1:8089 ./gateway`), then `scripts/smoke/gateway.mjs ws://127.0.0.1:8089 "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life`. It exercises the real console token read and auth verify; the `q` half needs the box's Redis (start events are written by the participant's Lambda), so it only asserts the membership refusal. Once `gw-dev.yyt.life` exists, pass `wss://gw-dev.yyt.life` instead. Check `/metrics` afterwards: every gauge and per-channel `connections` must be back at zero.
- catalog / asset smoke: with the same dev console deploy, `scripts/smoke/catalog.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)"` and `scripts/smoke/assets.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)"`. Both are self-cleaning (each run uses a timestamped app/bundle name) and both fetch the real CDN — the asset run is the only place that proves the presigned `Content-Type` is actually _signed_ (a substituted type must 403) and that CloudFront serves the committed object as `application/json` with `immutable`. Unit tests cannot see either, because both live in S3's signature and in the copy's metadata.
- Since todo/17 P3 every smoke script seats its synthetic member in a team of its own through `scripts/smoke/_team.mjs` (`ensureTeam`: find-or-create team `smoke-<script>` + project `game`; the auth seeder takes `projectId` so topic/match/dungeon channels can reference the seeded auth channel from the same project). The teams are **reused, never deleted**: a soft-deleted channel keeps its project undeletable until the daily sweep, so per-run teams would pile up. Residue per script is therefore one team, not one per run.
- Before the first deploy of a P3 bundle on a stage: `node scripts/apply-team-project-map.mjs <stage> local/team-project-map.<stage>.json` (dry run lists every unmapped app/bundle/channel), then `--execute`. The script refuses while any resource is neither assigned nor deleted; `deleteUnmappedChannels: true` deletes every unassigned channel, live or soft-deleted (the dry run prints the live count; on dev that was 300 rows of smoke residue, 1 live). Rows left unmapped are invisible to the new bundle (safe) but block the contract migration. Then `node scripts/split-projects-per-app.mjs <stage> local/split-projects.<stage>.json [--execute]` to split a catch-all project into one per app and backfill versions (`rules/data.md`).
- team smoke (teams/projects/versions/issues/discussions, todo/17 P2): with the same dev console deploy, `scripts/smoke/team.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)"`. Four synthetic members (admin/owner/mate/guest) run the whole membership state machine, the admin override, quotas and the markdown rate limit, then delete the project and team in a `finally` and demote the members back to `pending`. The rate-limit check fires four requests **concurrently**: sequential calls straddle the 500 ms slot behind Lambda latency and pass green without proving anything.
- match smoke: deploy auth, console and match on dev with `--param debugHooks=1`, then `scripts/smoke/match.mjs wss://match-dev.yyt.life "$(cat local/deploy/match-debug-http.dev)" "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life [--slow]` (`--slow` waits for the 1-minute tick to prove the partial timeout). The debug HTTP URL is the stack output `HttpApiUrl` (`aws cloudformation describe-stacks --stack-name yyt-match-dev`); keep it in `local/deploy/`. The script uses Node's global `WebSocket`, no `wscat` needed.
- state smoke: deploy console and auth on dev with `--param debugHooks=1`, `scripts/deploy.sh state dev`, then `scripts/smoke/state.mjs https://doc-dev.yyt.life "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life`. It creates its own auth channel, issues the doc key, exercises the whole CAS surface (428 / 409 with the winning version / create-over-existing / conditional delete) and both credentials' limits, then deletes the channel in a `finally` so a crashed run leaves nothing behind. It is also the only place that proves the _console→state_ wiring: the `docUrl` check fails when `doc-base-url` is missing from SSM.
- kv smoke (todo/33): `node scripts/smoke/kvstore.mjs https://doc-dev.yyt.life "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life`. Prerequisites in order, because two of them are invisible until the smoke fails: `scripts/bootstrap-ssm.sh <stage> state` (writes `kv-kek`) **before** `scripts/deploy.sh state <stage>`, since the KEK is baked into the Lambda env at deploy time; the state MySQL account granted `SELECT` on `kv_collections`, `SELECT, INSERT` on `kv_keys` and `SELECT, INSERT, UPDATE, DELETE` on `kv_entries` (an owner step in the ops repo, after `deploy.sh console <stage>` has applied `m0014_kvstore` — the tables must exist first). Five collections in the reused `smoke-kv` team — announcement (`project`/`team`), private progress (`user`/`user`), public profile (`project`/`user`, caps 4/1), an encrypted save, and a write-only inbox (`team`/`project`, the only shape that reaches the suppressed 201/ETag, the 403 on a conditional header and the idempotent 204 on a missing key) — carry the whole principal × scope matrix across a console session, a doc key and two player JWTs. It also proves what no unit test can: a second project's doc key gets 404 (not 403) on this project's collection, a seatless platform admin reads keys but never values, an encrypted value round-trips through both the single read and the per-row AAD of a listing, and deleting the auth channel takes its players' rows while the two rows the console wrote (shared and owner-namespace) survive. It deletes its collections and channels in a `finally` and demotes its members in a nested one; the team and its `game`/`kv-other` projects are reused like every other smoke's. The CLI half is a `yyt kv` round trip with the dev profile (2026-09-05, S7): `create --read project --write user --max-entries-per-owner 3` → `entry put` from `--value`, from stdin and with `--if-version` (a stale version prints `conflict: version mismatch {"current":N}`) → `entries` → `entry get` (raw value, no newline) → `update --max-entries` → `entry clear --owner` → `entry rm` → `delete`; the collection page in the SPA is the same rows at `/ui/kv/{id}`.
- Reading a red kv run: the console half and the API half fail for different reasons, and only the API half depends on the stage. A run where the ~40 console checks pass and the API ones answer `503 database error` is the **missing grant**; `503` with `kv_encryption_not_configured` is the missing or malformed `kv-kek`; `401` on a call that worked a moment ago is a doc key whose channel was deleted. "Every check fails" is never the KEK — `requireCrypto()` gates only the state stack's `/kv/*`, and console can still create even an encrypted collection (it refuses that one only when `doc-base-url` is unset).
- topic smoke: deploy auth and console on dev with `--param debugHooks=1`, `scripts/deploy.sh topic dev` (no debug hooks needed — `ttlSec` down to 1s makes expiry testable), then `scripts/smoke/topic.mjs https://topic-dev.yyt.life "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life`.
- Stage-wide SSM keys `github-client-id|secret` are required by the console stack; on dev they are placeholders until the operator registers the GitHub OAuth app (user task, `todo/10-prod-launch.md`).
- SPA (team model, 2026-08-26): after `scripts/deploy.sh console dev --param debugHooks=1` and `scripts/deploy-web.sh dev`, open `https://console-dev.yyt.life/ui/`, mint a session with `POST /debug/login` from the browser console (role `member` is enough; `admin` to see the seatless-admin read-only view), then walk: Teams → create team → project → Channels tab → new auth channel (secret shown once) → Versions bump → Issues create/close → Members add/kick (rotation notice) → Settings delete. Check the response headers of `/ui/` carry `content-security-policy` and that the console shows no CSP violation while uploading an artifact (the presigned S3 PUT must be allowed by `connect-src`).
- SPA (dev server): `YYT_DEBUG_KEY="$(cat local/deploy/debug-key.dev)" pnpm --filter @yyt/console-web dev` (proxies to console-dev, which must be deployed with `--param debugHooks=1`), then in the browser console `fetch("/debug/login", {method:"POST", headers:{"content-type":"application/json"}, body: JSON.stringify({login:"dev-admin", githubId:-901, role:"admin"})})` and reload. Walk: channel create (secret shown once) → extend → edit → event draft (title/place/hours/vote deadline/candidate dates) → publish → vote (tick several dates) → poster upload → edit page (revision + diff in "Page history") → comment → sign out → public page after the deadline. Clean up with the API afterwards (delete channels/tokens; `DELETE /events/{id}` as admin removes the event with its history; re-login the synthetic member with `role:"pending"`).
- Events smoke (redesign, 2026-08-29): `node scripts/smoke/events.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)"` — drives draft → publish → votes → poster replace → revisions → a real 20 s vote deadline → waiting/tally → cancel → admin delete, and deletes what it made in a `finally`. It picks candidate days ~400 days out (seeded by the minute) so it never collides with a real event's day; a `date_taken` on "member creates draft" means a leftover from an aborted run — list drafts as `smoke-admin` and delete them.
- Shows smoke (the gallery, 2026-09-01): `scripts/deploy.sh console dev --param debugHooks=1` and `scripts/deploy-web.sh dev`, then `node scripts/smoke/shows.mjs https://console-dev.yyt.life "$(cat local/deploy/debug-key.dev)"`. Four synthetic identities (admin/owner/grantee/pending) seated in the reused `smoke-shows` team drive: create + grant (an unknown login must answer exactly like a real one), one entry of each target kind, three screenshots presigned in **one** call and PUT to real S3, a two-of-three commit, the 302 serving the uploaded bytes under `no-store`, a full set of three still able to presign a replacement, the parent assertion through another show's id, a deleted target leaving its entry on a snapshot name, anonymous visibility after an ACL narrowing (the screenshot redirect included), the widening 409, close/reopen, admin moderation with and without a reason, and the audit read. It deletes its shows in a `finally` and demotes the members back to `pending`; the site it exhibits is deleted mid-run on purpose. `aws lambda invoke --function-name yyt-console-dev-expire` afterwards prints a `show sweep` line — that is the only check that the sweep is wired and that the `shots/` IAM works.
- Show CLI on dev (2026-09-01): mint a member session with `POST /debug/login` and a token with `POST /tokens`, then `YYT_CONFIG=<scratch> YYT_API=https://console-dev.yyt.life YYT_TOKEN=… YYT_TEAM=smoke-shows YYT_PROJECT=game yyt show …`. The run that matters is `yyt show entries submit <show> <title> --site <name> --screenshot a.png --screenshot b.png`: it exercises the batch presign, the bare-client PUT and the id-based commit in one command. Space the commands ~1 s apart or the per-member write slot answers `rate_limited` — that is the design working, not a failure. Clean up by deleting the show as an admin (a reason is required) and the site as its owner.
- CLI smoke (team model, 2026-08-26): mint a synthetic **member** session with console's `POST /debug/login` (`{login:"cli-smoke", githubId:-1101, role:"member"}`), `POST /tokens` with the cookie + `origin`, then drive the real binary with `YYT_CONFIG=<scratch> yyt login --api https://console-dev.yyt.life --token …` and walk: `team create` → `project create` (must fail: no context) → `team use` → `project create` → `project use` → `channels create --kind auth`, then `--kind match --auth-channel <auth name>` → `channels get <name>` → `catalog app create` → `asset create` + `asset push` → `project version create|bump|link` → `project issue create|close|comment add` → `team discussion create` → `team history`. Clean up in reverse; `project rm`/`team rm` answer `conflict` while the soft-deleted channels exist (until the sweep), so the `cli-smoke-*` team is the same one-team residue the smoke scripts leave. Revoke the token and re-login the member with `role:"pending"`. `yyt smoke match` needs the match stack deployed with `--param debugHooks=1` (callback sink `$(cat local/deploy/match-debug-http.dev)/debug/callback`) and JWTs from auth's `POST /debug/token`; config edits reach match only after its 60s channel cache expires. Full command log: `todo/13-cli-cata-parity.md`.
- Sample smokes (dungeon, morpg) and their client drivers moved to `yingyeothon/examples` gitignored `local/` on 2026-09-03 (`local/ops.sh <sample> setup|deploy|run|clean`, `local/smoke/`, `local/tools/`, env files under `local/env/`). Bullets tagged **[pre-move]** keep their lessons with the old paths: `scripts/smoke/<x>.mjs` → that repo's `local/smoke/<x>.mjs`, `examples/sample-<x>` → `sample-<x>`, `../../local/deploy/` → `../local/env/`.
- **[pre-move]** dungeon (tslib integration) smoke: needs a Redis ACL user for the sample (`game`×stage, `local/env/game.<stage>.env` with only `REDIS_*`), then `scripts/smoke/dungeon.mjs setup …` (seeds auth/match/topic channels, writes `local/deploy/dungeon.dev.env` + `dungeon-channels.dev.json`) → `examples/sample-dungeon/scripts/deploy.sh ../../local/deploy/dungeon.dev.env dev` → `scripts/smoke/dungeon.mjs run … <CallbackUrl>` → `clean`. Exact commands: `scripts/smoke/dungeon.mjs` usage text and `examples/sample-dungeon/README.md`. Gateway mode (sockets in the realtime gateway, verified 2026-08-27): `setup … <outEnv> <outState> gateway` (writes `local/deploy/dungeon-gw.dev.env` + `dungeon-gw-channels.dev.json` with a `q` channel and its participant credential) → deploy with that env → the same `run`; the dev gateway container must be running (`yyt-stateful/services.sh start dev`). A stack deployed in one mode answers every match callback in that mode, so keep one state file per mode and `run` the one matching the last deploy. `run` rewrites the match channel's `callbackUrl` (full-config PATCH), so a channel whose config the match service cached within the last 60 s fails the first match with `failed/callback` — wait a minute and rerun.
- Usage digest: `aws lambda invoke --function-name yyt-console-dev-expire /dev/stdout` then `aws logs filter-log-events --log-group-name /aws/lambda/yyt-console-dev-expire --filter-pattern '"usage"'` — expect a `redis usage` line and a `usage digest` line with `bucket` and `cdn` objects (verified on dev 2026-08-28). The SNS path is exercised only by a real warning; the unit tests cover the message shape.
- **[pre-move]** morpg (lobby + dungeon + doc store) smoke, verified 2026-08-27: `scripts/smoke/morpg.mjs setup "$(cat local/deploy/debug-key.dev)" https://auth-dev.yyt.life https://console-dev.yyt.life https://doc-dev.yyt.life local/deploy/morpg.dev.env local/deploy/morpg-channels.dev.json` (auth + lobby + q channel + participant credential + doc apiKey + map bundle asset) → `cd examples/sample-morpg && scripts/deploy.sh ../../local/deploy/morpg.dev.env dev` (keep the `ApiUrl` output in `local/deploy/morpg-api.dev`) → `scripts/smoke/morpg.mjs run … wss://gw-dev.yyt.life local/deploy/morpg-channels.dev.json "$(cat local/deploy/morpg-api.dev)"` → `clean`. Since 2026-08-28 `run` uses `@yingyeothon/gamebase-client` for every socket (re-verified ALL OK on dev that day, gateway 0.2.0, tslib 2.0.0 from npm; again with 2.0.1 the same day). `clean` deletes the channels, so `local/deploy/morpg-channels.dev.json` is stale afterwards: a later `run` fails at the first socket with a bare `ws error` (the gateway answers 404 `channel not found` on the upgrade) — rerun `setup` and redeploy the stack first. `local/deploy/morpg-api.dev` is written by hand from the deploy's `ApiUrl` output; verified again 2026-08-28 against gateway v0.2.0 on dev. The dev gateway container must run an image with `GET /parties` (gateway ≥ the 2026-08-27 route); until it does, `run` stops at `a member enters` with `party_not_found`. To verify without touching the box: run the gateway binary locally on `127.0.0.1:8089` with the dev gateway's Redis credential (from the private ops repo's env, never printed) and `GATEWAY_CONSOLE_URL=https://console-dev.yyt.life`, then serve the game's HTTP API locally with `examples/sample-morpg/scripts/local-api.mjs 8090` (env file + `GATEWAY_WS_URL="ws://127.0.0.1:8089/?channel=<qId>"`, `CALLBACK_BASE_URL=<deployed ApiUrl>`, `GAME_ACTOR_LAMBDA_NAME=yyt-sample-morpg-dev-actor`, `AWS_PROFILE=yyt`) and point `run` at `ws://127.0.0.1:8089` / `http://127.0.0.1:8090`: the actor, the q bridge's Redis and the doc store are still the real dev ones, only the sockets and the entry Lambda are local. Kill the local processes by pid afterwards (`pgrep -x gateway`, `pgrep -f "^node scripts/local-api.mjs"`) — a `pkill -f` pattern that also matches the calling shell kills the session.
- **[pre-move]** morpg bundle changes (2026-08-29): the shipped bundles are format 2 (`templates` inlined in `assets/zone001.json`, a field-only `zone002.json`). After editing `assets/*.json`: `scripts/smoke/morpg.mjs publish-map "$(cat local/deploy/debug-key.dev)" https://console-dev.yyt.life local/deploy/morpg-channels.dev.json local/deploy/morpg.dev.env v<N>` (uploads every file under one new immutable version, repoints the lobby's `mapUrl`, rewrites `MAP_URL` and `state.mapUrl`), then redeploy the stack — the http Lambda caches the bundle per container, so a repoint without a redeploy serves the old templates until the containers recycle. `clean` deletes every version the state file lists. A user id for curl checks must be 32 hex chars (`doc.ts` refuses others with `invalid ownerId` → 502). To exercise the sheet routes by hand: mint a debug token, `/talk hunter` (accept), seed items through the doc store (`PUT {DOC_BASE_URL}/s/{sub}` with the game's apiKey and `if-match: "<version>"`), then `/inventory/wooden_sword/equip` (attack +5), `/inventory/rage_scroll/use` (attack +10, 300 s), `/npc/elder/interact` (turns in `jelly_gather`), `/npc/forest_gate/interact` (`teleported`, `mapUrl` of zone002) — verified ALL as designed on dev 2026-08-29.
- **[pre-move]** morpg batch drive (primary machine check since 2026-08-29): `python3 local/tools/morpg-batch-drive.py` (machine-local) runs alice and bob as `pnpm play -- --batch` subprocesses, reads their NDJSON on threads, walks the bots to the boss with a BFS over the bundle grid and asserts the loop (party → `/enter` at the entrance → `stage: running` → kill → `run over` → `commit`). Exit 3 from either client = a `/wait` timed out; read its last `wait` object. Two `--batch` lessons (2026-08-29): the readline interface must be created when the script loop starts, not before the session connects — lines piped in early are emitted with no listener and lost (the drive passed only because it writes after `lobby connected`; `printf … | pnpm play --batch` hung); and the process must `process.exit` after the `quit` line — a redirected stdin or an SDK timer keeps the event loop alive. A same-zone `/zone` must keep the peer map (the gateway resends a snapshot only for a zone change) — both drivers start with `/zone zone001` and saw an empty zone. `--batch` prints every log line and `/ls` rows as JSON, so no screen scraping; a solo `/enter` starts an actor that lives `GAME_RUNNING_SECONDS` and blocks the party with `409 party_in_dungeon` until then.
- **[pre-move]** morpg terminal client (`examples/sample-morpg/cli`, 2026-08-28): same `setup` → deploy prerequisites as the smoke (the CLI needs live lobby/q channels, so after a smoke `clean` rerun `setup` and redeploy), a gitignored `local/deploy/morpg-cli.dev.env` (`MORPG_API_BASE`, `MORPG_GATEWAY_WS_URL=wss://gw-dev.yyt.life`, `MORPG_AUTH_BASE`, `MORPG_STATE_FILE`, `MORPG_DEBUG_KEY_FILE` as paths under `local/deploy/`), then two terminals `pnpm play -- --config ../../local/deploy/morpg-cli.dev.env --user alice|bob`. Hands-off version: `python3 local/tools/morpg-cli-drive.py` (machine-local) forks both clients on ptys, reads the ANSI screen back, walks to the boss and asserts the whole loop (party → `/enter` announce + 10 s window → `dungeon.start` relay → kill → result → town → sheet reloaded → re-enter) — ALL OK on dev 2026-08-28 in ~50 s (again after the gamebase-client 2.0.1 bump, `dir` on the wire). Set `GAME_RUNNING_SECONDS` ≥ 240 in the deploy env for a human party; the smoke's 120 s is tight. The gateway keeps party membership across sessions, so a scripted run starts with `/party leave` on both. Since 2026-08-29 the driver also covers NPCs (`/talk hunter` ×3 → two accepts then `quest_incomplete`), the kill-quest progress on the sheet, and the forest gate (`/talk forest_gate` on both → zone002 grid + `town_gate`, peers visible there, `/enter` → `dungeon … on zone002` with wolves drawn from the field bundle, `/talk` refused inside a run). A driver run leaves both sheets in zone002 and a live wolf-field actor (`GAME_RUNNING_SECONDS`), so it starts with `/zone zone001` on both and the next run must wait for that actor to end (`409 party_in_dungeon` otherwise). Send zone/NPC commands and wait for their log line _before_ scripted moves: the answer re-positions the player at the zone start when it lands.
- morpg smoke since 2026-08-29 covers the sheet routes, the reconnect, potions and the single-session rule in one `run` (pass the deploy env file as the 7th argument for the potion checks; they print `skip` without it) and the running-stage timeout in `timeout` mode (takes `GAME_RUNNING_SECONDS`, 240 s on dev). `run` leaves two sheets behind (fresh ids each run, nothing to clean) and no running actor; `timeout` ends its actor by itself. Two lessons from the same day: a stateful-host restart mid-smoke shows up as close `1001` on every socket and a doc-store 503 — the smoke's SDK clients run with retries off, so re-run after the host is back rather than reading the failures; and, with tslib ≤ 2.0.1, a Redis restart leaves the sample stack's warm Lambda containers answering `502 upstream` (`-NOAUTH` inside) until `scripts/deploy.sh <env> dev --force` recycles them (`deploy.sh` forwards extra serverless arguments; a plain deploy says "No changes" and recycles nothing). Ask the user whether the host is being restarted before chasing a `1001`/`1006` that only one run showed.
- **[pre-move]** Redis-restart recovery of the sample stacks (tslib `naive-redis` AUTH/NOAUTH reconnect + `naive-socket` ended-socket reconnect, both unpublished on `main` after 2.0.1; verified 2026-08-29 through `link-service.mjs`): run both smokes once so the containers are warm, restart the Redis store from the private ops repo's service script (one shared instance — prod games drop for a few seconds too, so ask first), wait ~5 s and run the same smokes again; both must print ALL OK on the first try and `aws logs tail /aws/lambda/yyt-sample-{morpg,dungeon}-dev-* --since <restart>` must show no `Invoke Error`. Before the second fix the post-restart run failed with `actor_not_ready` and the actor logged `EPIPE` / `This socket has been ended by the other party` from `saveActorStartEvent`: the frozen container resumed with the server's FIN undispatched and wrote to the dead socket. `NaiveSocket` logs are not surfaced by the samples, so the evidence is the outcome, not a reconnect line. `scripts/smoke/dungeon.mjs run` takes the **match** service socket (`wss://match-dev.yyt.life`), not the gateway URL, and its state file goes stale after `clean` like morpg's (404 on the callbackUrl PATCH → rerun `setup … gateway`, redeploy, wait 60 s for the channel cache).
- **[pre-move]** morpg field potion / effective stats on dev (2026-08-29): `node local/tools/morpg-potion-check.mjs` (machine-local; reads `local/deploy/*`) seeds a sheet with an equipped sword + a live `rage` + two potions through the doc store, checks `GET /character` `effective`, enters a solo party, expects `full_hp`, walks to the boss and hits it **once** (monsters only retaliate, and an effective attack of 25 one-shots a slime, so a slime never hits back), then expects a `heal` event. It leaves a running actor behind (`GAME_RUNNING_SECONDS`), so the next run of the CLI driver must wait it out or use fresh users.
- morpg sheet routes on dev (2026-08-28): mint a debug JWT for a throwaway `--user`, then curl `POST /character/stats-up` etc. against `local/deploy/morpg-api.dev`. A fresh sheet has 0 stat points, so seed one first through the doc store with the game's apiKey (`PUT {DOC_BASE_URL}/s/{sub}` from `local/deploy/morpg.dev.env`, `if-match: "0"`, a format-2 body with `statPoints`) and watch `version` climb per write; refusals must leave it unchanged.
- The match smoke's second argument is the match stack's **HTTP** API (`HttpApiUrl` output), not `https://match-dev.yyt.life` — that hostname is the WebSocket API and answers `426 Upgrade Required` to the callback POST, which surfaces as three unrelated-looking failures. Its `debug` function is capped at `reservedConcurrency: 1`, so reading back the recorded callback immediately after the match can lose the slot to the callback itself and answer a bare `{"message":"Service Unavailable"}` (API Gateway's throttle shape, not ours); `--slow` avoids it.
- match protocol gotcha for clients: a `ping` that arrives after the match already formed (ticket consumed) is answered with `{type:"failed", reason:"closed"}`. Smoke clients should not ping right after connecting; connecting alone enqueues the player.
- Console cookie sessions are CSRF fail-closed: any state-changing smoke/curl call that authenticates with the session cookie must also send `origin: <consoleBase>` or it 401s (topic/match smokes learned this after the fail-closed change).
- The match dev stack loses its HTTP API (and the `HttpApiUrl` output) whenever it is deployed without `--param debugHooks=1`; `local/deploy/match-debug-http.dev` then points at a host that no longer resolves (`ENOTFOUND`). Redeploy with the param and rewrite the file from the stack output (the `POST /debug/callback` receiver goes with it — every match then ends `failed:callback`); keep the flag in the per-stage deploy note under `local/deploy/`. Before blaming the smoke script.
- SPA browser checks: prefer the dev server (`YYT_DEBUG_KEY=… pnpm --filter @yyt/console-web dev`) over pasting the debug key into the deployed page's console — the proxy attaches the key server-side, so the secret never enters the browser or a transcript. Verify the CSP/`x-frame-options` headers of the deployed `/ui/` separately with `curl -D -`. Use a fresh negative `githubId` per synthetic member: `POST /debug/login` upserts by `githubId`, so reusing one silently renames another script's member (`-1102` belongs to the team smoke's owner).
- Names are unique per team (`m0008_team_project_contract`): the same app/bundle/channel name in two teams is allowed, the same name twice in one team is 409 (`duplicate key`), and a soft-deleted channel keeps its name until the sweep purges it.
- version auto-link (2026-09-02): after `catalog artifact upload … --version <v>` the repo-built CLI prints `linked to version <v> (<id>)` (and `created version …` on the first upload of that version) on stderr, `yyt project version ls` shows `ARTIFACTS 1`, and `yyt project issue create t --version <new name>` prints `created version …` once and reuses the row on the next call; `node scripts/smoke/catalog.mjs` / `team.mjs` cover the same on dev (the catalog smoke deletes its `1.0.0`/`2.0.0` versions before and after, since the team is reused across runs).
- console app (Flutter `apps/console-app`, published as catalog app `platform/console`): `cd apps/console-app && flutter analyze && flutter test && flutter build apk --release` (signing from gitignored `android/key.properties`), then upload with the **repo-built** CLI (`cd cli && go build -o yyt ./cmd/yyt`; a stale `~/.local/bin/yyt` predates `--team/--project`): `yyt --team platform --project console catalog artifact upload console build/app/outputs/flutter-apk/app-release.apk --platform android --version <pubspec> --tag build_type=release --tag application_id=life.yyt.console --tag title=잉여톤`, and confirm `GET /catalog/installer/downloads` lists the new version first. The app compares that route with its own `package_info_plus` version at launch (`lib/self_update_check.dart`) and shows the update banner; the only way to see the banner is a device running an older build against a stage whose newest upload is newer, so bump `pubspec.yaml` before every upload. Installing on a device and the self-update from the previous build are manual.
- CLI self-update: `go build -o <scratch>/yyt ./cmd/yyt && <scratch>/yyt self update --check && <scratch>/yyt self update` replaces the scratch copy with the newest GitHub release ; never run it on `~/.local/bin/yyt` while checking an unreleased build, or it downgrades to the release.
- Channel config edits reach match/topic only after their 60 s `chcfg` Redis cache expires: a smoke that rewrites `callbackUrl` right before connecting gets `failed/callback` on the first match — wait a minute and rerun before debugging.
- Do not send `ping` right after `matched` in a match smoke: the server has already released the ticket and answers `failed:closed`.

## Console app (Flutter) release

- Build: `cd apps/console-app && flutter test && flutter build apk --release` (signing from gitignored `android/key.properties`); publish with the `yyt … catalog artifact upload` line in `apps/console-app/README.md` (`--profile prod` for prod). Re-uploading the same `version` tag needs the old artifact deleted first.
- "배포" for the console app (2026-08-27) means exactly: patch-bump `pubspec.yaml` (both `x.y.z` and `+build`), `flutter analyze && flutter test && flutter build apk --release`, upload to **dev and prod** with the repo-built CLI, confirm `GET /catalog/installer/downloads` on both — the recipe is `apps/console-app/README.md` "Release". `android/key.properties` is gitignored and not in `local/`; if it is missing, the release build silently falls back to the debug key and devices refuse the update (signature mismatch), so check for the file before building.
- Install-flow verification cannot be unit-tested end to end: `waitForInstalledVersion` is exercised with fake `findInstalledVersionFn` + completer signals (`test/install_verification_test.dart`), the lifecycle observer is manual. Manual checklist for a release touching it: cancel at the install prompt, deny unknown-sources then return, background the app during the prompt, back out of "Installing…" on a large APK — each must end the dialog within `returnGrace` (30 s) with the right message.
- Console timestamps are unix **seconds**; a Dart model that feeds them to `fromMillisecondsSinceEpoch` renders 1970 and unit tests with made-up millisecond fixtures will not catch it — assert the decoded `DateTime`.
- When an app build starts calling a **new** console route, deploy console (dev, then prod) before uploading the APK, and have the client fall back on 404 `not_found` (`ProjectsApi.listTeamIssuesCompat` walks `/projects/{id}/issues` when `/teams/{id}/issues` is missing) so a phone that updates first still works. Smoke checks that assert order by `updated_at` must space the writes ≥1.1 s apart: timestamps are whole seconds and the tie-break is a random id.
- The installed build finds its own update through the app list route it was built with; never remove a list route until every device has moved to a build that uses the replacement (todo/17 P10).
- **[pre-move]** morpg two-terminal play (2026-08-29, `todo/20` phase 7): the driver can run back to back — it starts with `/zone zone001` and its second run waits for the previous wolf-field actor. `pnpm play -- --config … --user x` is the documented form; pnpm 10 passes the `--` through and the CLI ignores it. After killing both clients of a live run, the actor keeps ticking until `GAME_RUNNING_SECONDS`; a Redis command that times out (1 s) in that window ends the run with `reason: error` and logs an `Invoke Error` after `onGameEnd` (tslib, `todo/19` A) — count such lines with `aws logs tail /aws/lambda/yyt-sample-morpg-dev-actor --since <start> | grep -c "Invoke Error"` before blaming a new change.
- **[pre-move]** pty-driven screen checks after the 2026-08-29 terminal change (`local/tools/morpg-cli-drive.py`): the client now paints changed rows only and never blocks on the terminal, so the driver must (1) read every client's pty continuously (a reader thread — the old driver only read the client it was waiting on, which used to freeze the other client inside its synchronous write and, with the fix, lets it run ahead: auto-return fires, a space becomes "interact"), (2) model cursor addressing (`Screen` class) instead of scanning for full repaints, (3) assert on panel rows (`game: g_…`) rather than log lines — a log line that scrolls out inside one 100 ms paint tick is never written — and wait for a _new_ value rather than matching the cumulative output, and (4) expect `already in <zone>` for a same-zone `/zone`. `MORPG_TRACE_DIR=<dir>` makes the driver pass `--trace` per client; the trace, not the screen, is what explains a silent step.
- **[pre-move]** morpg web client on dev (2026-08-30): `cd examples/sample-morpg && MORPG_CONFIG=../../local/deploy/morpg-cli.dev.env pnpm web --host 127.0.0.1 --port 5174` (same env file as the terminal client; the dev server mints the debug token itself), then `node local/tools/morpg-web-drive.mjs` (machine-local; playwright from the npx cache + system Chrome, headless) plays a browser `webalice` against a `--batch` terminal `webbob`: keys move, `/ls players` from the terminal lists the browser, chat both ways, invite by name → accept, `/enter` → announcement → `stage: running` on the canvas, attack/target/skill keys, the canvas is fully painted from the sheets (`getImageData` alpha count), no `view_problem`/`view_fail` trace and no page errors — ALL OK, screenshots in `/tmp/morpg-web/`. `MORPG_NO_CORS=1` launches the test browser with `--disable-web-security`: use it only while the stage's asset CDN still lacks the CORS response-headers policy (`rules/deployment.md`); the platform fix is verified by `scripts/smoke/assets.mjs`, not by this flag. Each run leaves a running actor for the party (`GAME_RUNNING_SECONDS`); use fresh names (`ALICE=… BOB=…`) to run twice inside that window.
- morpg web sign-in (2026-08-30): the hosted page needs the auth channel to carry (a) the page URL as origin+path in `redirectAllowlist` (`https://dev-g.yyt.life/<slug>/`, plus `http://127.0.0.1:5174/` for the vite dev server) and (b) a GitHub OAuth app **of its own** whose callback is the channel's `callbackUrls.github` — the console's OAuth app cannot be reused (a GitHub OAuth app has one callback URL, and the console's is `console…/auth/github/callback`; a game channel token is issued by the auth stack, never by console); set both with `yyt channels update <auth> --redirect … --github-client-id …` and pass the secret through `GITHUB_CLIENT_SECRET` from SSM (never on the command line or in output). `curl -D - "$startUrl?provider=github&redirect=…"` proves the allowlist (302 to `github.com/login/oauth/authorize` vs 403), but GitHub checks `redirect_uri` only after the user signs in, so a mismatch with the app's registered callback shows up in a browser, not in curl. The `dbg_` channel from `morpg.mjs setup` starts with no provider and an empty allowlist — re-apply both after every `setup`.
- Browser UI on phones: emulate a coarse pointer (playwright `devices["Pixel 7"]`) as well as the desktop drive — the touch pad is `pointer: coarse` only, and a code path nobody calls (`pad.show(true)` was missing until 2026-08-30) passes every desktop check. Measure element heights when a layout looks off: `white-space: pre` on a container renders the markup's own newlines.
- morpg web game UI (2026-08-30): `morpg-web-drive.mjs` now opens the debug view from the menu before reading `#side`/`#log` (the panels are painted while hidden, so `textContent` works either way), drives the HUD by `data-testid` (`btn-primary|bag|char|quests|party|chat|menu|debug|command`, `[data-testid=dialog][data-dialog=talk|inventory|…]`, `[data-testid=row]`, `toast-invite|pending|result|line`), checks NPC collision (2,1 → east into the hunter stays), player pass-through (bob's start cell), and ends with a coarse-pointer context (`hasTouch`, 412×915) that drags the joystick, taps a disabled button (`aria-disabled`: Playwright needs `click({ force: true })`) and switches popups from the icons. Position reads come from `#side` (`@x,y`) — while a popup is open the panel shows the menu, so count `dispatch move` trace events instead. **The hosted build cannot be driven once `login` is configured**: the gate hides the token field and the connect button behind the sign-in buttons, so `MORPG_WEB_URL=https://dev-g…` stops at the first wait; verify the host with `curl` (markup + `config.json`) and sign in from a browser by hand.
- The sample's vite dev server needs `MORPG_CONFIG=<env-file>`; without it `/__morpg/config` answers 500 and the page reports "no dev config". Check `ss -ltnp` for a stale dev server on 5174 before trusting a drive result (`--strictPort`).
- Exercise the OAuth callback path itself, not only the button path: load the page, set the nonce in `sessionStorage`, then navigate to `?login=<nonce>#token=<debug jwt>&userId=…&exp=…` and watch `pageerror`. The auto-connect on the callback runs during module evaluation, where a `let`/`const` declared further down is a TDZ `ReferenceError` (minified: "Cannot access 'de' before initialization") that the click path never hits — it shipped twice before it was caught (2026-08-30). Keep top-level calls at the end of `web/src/main.ts`.
